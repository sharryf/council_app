<?php

/**
 * Overrides just the "List" breadcrumb — see lang/vendor/filament-panels/
 * dv/layout.php's own comment for why this is a deliberate, narrowly
 * scoped override (only the strings actually shown on-screen) rather
 * than a wholesale translation of every Filament package string.
 */
return [

    'breadcrumb' => 'ލިސްޓް',

];
