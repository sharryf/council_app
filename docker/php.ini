upload_max_filesize = 50M
post_max_size = 55M
memory_limit = 512M
max_execution_time = 120
expose_php = Off

; The image never changes while it runs, so PHP can skip re-checking files.
opcache.validate_timestamps = 0
